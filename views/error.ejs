<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Error' %> - SkyNest Hotels</title>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    
    <style>
        body {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        
        .error-container {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        
        .error-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            padding: 3rem;
            text-align: center;
            max-width: 600px;
            margin: 2rem;
        }
        
        .error-icon {
            font-size: 4rem;
            color: #dc3545;
            margin-bottom: 1.5rem;
        }
        
        .error-title {
            font-size: 2.5rem;
            font-weight: 700;
            color: #333;
            margin-bottom: 1rem;
        }
        
        .error-message {
            font-size: 1.2rem;
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
        }
        
        .btn-home {
            background: linear-gradient(45deg, #667eea, #764ba2);
            border: none;
            padding: 12px 30px;
            border-radius: 50px;
            color: white;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s ease;
            display: inline-block;
        }
        
        .btn-home:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(102, 126, 234, 0.3);
            color: white;
            text-decoration: none;
        }
        
        .error-details {
            background: #f8f9fa;
            border-radius: 10px;
            padding: 1.5rem;
            margin-top: 2rem;
            text-align: left;
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
            color: #495057;
        }
        
        .hotel-logo {
            color: #667eea;
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 2rem;
        }
        
        @media (max-width: 768px) {
            .error-card {
                padding: 2rem 1.5rem;
                margin: 1rem;
            }
            
            .error-title {
                font-size: 2rem;
            }
            
            .error-icon {
                font-size: 3rem;
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <div class="error-card">
            <div class="hotel-logo">
                <i class="fas fa-hotel"></i> SkyNest Hotels
            </div>
            
            <div class="error-icon">
                <% if (error.status === 404) { %>
                    <i class="fas fa-map-marker-alt-slash"></i>
                <% } else if (error.status === 403) { %>
                    <i class="fas fa-lock"></i>
                <% } else if (error.status === 500) { %>
                    <i class="fas fa-exclamation-triangle"></i>
                <% } else { %>
                    <i class="fas fa-exclamation-circle"></i>
                <% } %>
            </div>
            
            <h1 class="error-title">
                <% if (error.status === 404) { %>
                    Oops! Page Not Found
                <% } else if (error.status === 403) { %>
                    Access Denied
                <% } else if (error.status === 500) { %>
                    Server Error
                <% } else { %>
                    Error <%= error.status || 500 %>
                <% } %>
            </h1>
            
            <p class="error-message">
                <% if (error.status === 404) { %>
                    The page you're looking for seems to have checked out. Let's get you back to our main lobby.
                <% } else if (error.status === 403) { %>
                    You don't have permission to access this area. Please contact our front desk for assistance.
                <% } else if (error.status === 500) { %>
                    We're experiencing some technical difficulties. Our team has been notified and is working to resolve the issue.
                <% } else { %>
                    <%= error.message || 'Something went wrong. Please try again later.' %>
                <% } %>
            </p>
            
            <div class="mb-3">
                <a href="/" class="btn-home">
                    <i class="fas fa-home me-2"></i>Return to Homepage
                </a>
            </div>
            
            <div class="mt-3">
                <a href="javascript:history.back()" class="text-muted text-decoration-none">
                    <i class="fas fa-arrow-left me-1"></i>Go Back
                </a>
            </div>
            
            <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
                <div class="error-details">
                    <h6><i class="fas fa-bug me-2"></i>Development Details:</h6>
                    <strong>Status:</strong> <%= error.status || 500 %><br>
                    <strong>Message:</strong> <%= error.message %><br>
                    <% if (error.stack) { %>
                        <strong>Stack Trace:</strong><br>
                        <pre class="mt-2 mb-0"><%= error.stack %></pre>
                    <% } %>
                </div>
            <% } %>
        </div>
    </div>
    
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>